import type { MantineThemeOverride } from '@mantine/core'

const theme: MantineThemeOverride = {
  colorScheme: 'dark',

  headings: {
    fontFamily: "'DM Sans', 'Lilita One', sans-serif",
    sizes: {
      h1: { fontSize: 30 },
    },
  },
}
export default theme
